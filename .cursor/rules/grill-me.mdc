---
description: >-
  围绕 plan 或 design 持续追问，直到达成 shared understanding。用户说
  "grill me" / "考我" / "压测设计"时启用（勿因普通「有疑问追问」误触发）。
  提问必须用 Cursor AskQuestion 互动勾选。
globs: 
alwaysApply: false
---

围绕这个计划的每个方面持续追问我，直到我们达成共同理解。沿着 design tree 的每个分支走下去，一次解决一个决策之间的依赖。

## 提问方式

每次提问**必须**使用 Cursor 的 **`AskQuestion`（互动勾选）**，禁止用纯文字编号/列表冒充选择题。

1. **提供选项**：每个问题 2–4 个合理预设选项；`options` 每项含 `id` + `label`。
2. **末项自定义**：预设之后始终加一项，label 形如 `✏️ 其他（请说明）`。
3. **多选**：可能多答案时设 `allow_multiple: true`。
4. **推荐答案**：在推荐项 `label` 加 `⭐ 推荐`，或在消息里说明推荐及理由。
5. **一次一问**：每次只问一个问题，`questions` 只放一题；等用户回答后再继续。
6. **追问节奏**：得到回答后沿 decision tree 分支继续深挖，仍用 `AskQuestion`。

仅当 `AskQuestion` 不可用时，才回退为 markdown 选项列表（末项仍保留 `✏️ 其他（请说明）`）。

## 其他规则

- 每个问题都要说明推荐答案及理由。
- 能靠探索 codebase 回答的，去探索，不要提问。
- 优先用选项覆盖常见回答，减少用户打字负担。
