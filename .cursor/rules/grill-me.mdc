---
description: 围绕 plan 或 design 持续追问，直到达成 shared understanding。用户说"grill me"或"考我"时启用。
globs: 
alwaysApply: false
---

围绕这个计划的每个方面持续追问我，直到我们达成共同理解。沿着 design tree 的每个分支走下去，一次解决一个决策之间的依赖。

## 提问方式

每次提问必须使用 markdown 选项列表，按以下规则构造：

1. **提供选项**：为每个问题提供 2-4 个合理的预设选项，覆盖最可能的回答。每个选项用 `- ` 列表呈现，使用清晰的短文本。

2. **最后一个选项为自定义输入**：在预设选项之后，始终添加一个形如 `✏️ 其他（请说明）` 的选项，方便用户自由输入。

3. **多选提示**：如果问题可能有多个正确答案，在问题末尾注明"（可多选）"。

4. **推荐答案**：在你推荐的选项旁标注 `⭐ 推荐`。

5. **一次一问**：每次只问一个问题，等待用户回答后再继续。

6. **追问节奏**：得到回答后，基于答案沿着 decision tree 的对应分支继续深挖，每次仍遵循以上格式。

## 示例

当追问技术选型时：
```
你打算用什么数据库？

- PostgreSQL ⭐ 推荐
- SQLite
- MongoDB
- ✏️ 其他（请说明）
```

当追问需要考虑的方面时：
```
这个方案需要考虑哪些非功能需求？（可多选）

- 性能 / 延迟
- 安全性 ⭐ 推荐
- 可扩展性
- 运维成本
- ✏️ 其他（请说明）
```

## 其他规则

- 每个问题都要说明你的推荐答案及理由。
- 如果某个问题可以通过探索 codebase 回答，就去探索 codebase，而不是提问。
- 优先用选项覆盖常见回答，减少用户打字负担。
